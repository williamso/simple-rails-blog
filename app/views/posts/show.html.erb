

<%= render partial: "header", locals: { search: true} %>

<%= render 'cover' %>

<section id="posts" class="posts">
	<div id="posts-inner" class="posts-inner">
		<br><br><br><br>
		<% if notice.present? %>
			<!--<p id="notice"><%= notice %></p>-->
			<div class="bg-primary clearfix padder m-b">
        		<h4 class="m-b" style="color: #fff; text-rendering: optimizeLegibility;">Sua oportunidade foi enviada para nosso Adminstrador, para aprovação. Em até 20 minutos ela estará no ar. </h3>
        		<h4 class="m-b" style="color: #fff; text-rendering: optimizeLegibility;"> Para remover ou editar envie um e-mail para o Administrador (admin@hunterco.com.br), com o ID - <%=@post.id%> com a sua solicitação.</h4>
        		 </div>
		<% end %>
     

		<%= render @post %>

	</div>
</section>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
